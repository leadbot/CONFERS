name: CONFERS
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy
  - pandas
  - matplotlib
  - tqdm
  - joblib
  - biopython
  - mdtraj
  - tensorflow=2.10  # Pinning this to match your source env version is crucial for loading models